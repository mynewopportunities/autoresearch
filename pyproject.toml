[project]
name = "email-optimizer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.75.0",
    "requests>=2.32.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
